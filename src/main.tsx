import { StrictMode, useEffect, useMemo, useState, type ReactNode } from 'react';
import { createRoot } from 'react-dom/client';
import { MapContainer, TileLayer, useMap } from 'react-leaflet';
import { AMEX_URL, hasCoordinates, parseSnapshot, safeUrl, type Restaurant, type Snapshot } from './model';
import 'leaflet/dist/leaflet.css';
import './style.css';
import { ClusteredPins } from './ClusteredPins';
import { filterRestaurants, filterOptions, type Filters } from './filters';
const EMPTY_FILTERS: Filters = {query:'',district:'',cuisine:''};

function External({href, children, className}: {href?:string;children:ReactNode;className?:string}) {
 const url = safeUrl(href); return url ? <a href={url} className={className} target="_blank" rel="noopener noreferrer">{children}<span aria-hidden="true"> ↗</span></a> : null;
}
function Details({r}: {r:Restaurant}) {
 const phone = r.phone?.replace(/[\s().-]/g, '');
 return <div className="details"><p className="eyebrow">{r.district || '香港'} · {r.cuisine || '餐飲禮遇'}</p><h3>{r.name}</h3>{r.nameEn && r.nameEn !== r.name && <p className="english">{r.nameEn}</p>}<p className="address">{r.address}</p>{r.coordinates?.precision === 'building' && <p className="hotel-note">約略位置 · 按建築物定位，並非餐廳入口。請以餐廳地址為準。</p>}{r.isInHotel && <p className="hotel-note">酒店內餐廳 · 如入住該酒店，需於餐廳直接付款，不能掛帳於房間；以官方條款為準。</p>}{!hasCoordinates(r) && <p className="hotel-note">待定位 · 尚未有可靠座標，請參考地址或開啟 Google 地圖。</p>}<div className="detail-links"><External href={r.website}>餐廳官網／訂座</External>{phone && /^\+?[\d]{6,15}$/.test(phone) && <a href={`tel:${phone}`}>致電 {r.phone}</a>}<External href={r.googleMapsUrl}>Google 地圖</External><External href={AMEX_URL}>美國運通官方禮遇</External></div></div>;
}
function ViewControl({restaurants, reset}: {restaurants:Restaurant[];reset:number}) {
 const map = useMap();
 useEffect(() => { map.closePopup(); const points = restaurants.filter(hasCoordinates).map(r=>[r.coordinates!.lat,r.coordinates!.lng] as [number,number]); if(points.length) map.fitBounds(points,{padding:[45,45],maxZoom:14,animate:false}); else map.setView([22.31,114.17],11); },[map,restaurants,reset]);
 useEffect(()=>{ const observer = new ResizeObserver(()=>map.invalidateSize()); observer.observe(map.getContainer()); return ()=>observer.disconnect(); },[map]);
 return null;
}
function App() {
 const [data,setData]=useState<Snapshot|null>(null), [error,setError]=useState(false), [attempt,setAttempt]=useState(0), [selected,setSelected]=useState<string|null>(null), [revision,setRevision]=useState(0), [reset,setReset]=useState(0), [tileError,setTileError]=useState(false), [view,setView]=useState<'list'|'map'>('list');
 useEffect(()=>{const controller=new AbortController(); setError(false); fetch(`${import.meta.env.BASE_URL}data/restaurants.json`,{signal:controller.signal}).then(r=>{if(!r.ok)throw Error('load');return r.json();}).then(parseSnapshot).then(setData).catch(e=>{if(e.name!=='AbortError')setError(true);});return()=>controller.abort();},[attempt]);
 const [filters,setFilters]=useState<Filters>(EMPTY_FILTERS);
 const visible=useMemo(()=>filterRestaurants(data?.restaurants ?? [],filters),[data,filters]);
 const mapped=useMemo(()=>visible.filter(hasCoordinates),[visible]);
 const districts=useMemo(()=>filterOptions(data?.restaurants ?? [],'district'),[data]);
 const cuisines=useMemo(()=>filterOptions(data?.restaurants ?? [],'cuisine'),[data]);
 const active=visible.find(r=>r.id===selected);
 function updateFilters(next:Filters){setFilters(next);setSelected(null);}
 function clearFilters(){updateFilters(EMPTY_FILTERS);setReset(n=>n+1);}

 function select(r:Restaurant,fromList=false){setSelected(r.id);setRevision(n=>n+1);if(fromList && hasCoordinates(r))setView('map');}
 return <><a className="skip-link" href="#restaurant-list">跳至餐廳名單</a><header className="masthead"><a className="brand" href="./" aria-label="餐桌地圖首頁"><span className="brand-icon" aria-hidden="true">✳</span> 餐桌地圖<span className="brand-en">THE DINING ATLAS</span></a><External href={AMEX_URL} className="source-link">官方禮遇名單</External></header><main><section className="intro"><div><p className="eyebrow">HONG KONG / DINING COLLECTION</p><h1>下一席，<em>在香港。</em></h1><p className="intro-copy">從一頓精緻晚餐，到一次城市探索。<br className="mobile-break"/> 在地圖上，找到你的美國運通餐飲禮遇餐廳。</p></div><div className="edition"><span>香港餐飲指南</span><strong>一城・百味</strong><small>獨立整理 · 非官方網站</small></div></section>
 {!data ? <section className="load-state" aria-live="polite">{error?<><h2>暫時無法載入餐廳名單</h2><p>請檢查網絡連線，然後重試。你亦可直接參考官方名單。</p><button onClick={()=>setAttempt(n=>n+1)}>重新載入</button></>:<><span className="loading-dot"/><h2>正在準備你的餐桌地圖…</h2></>}</section> : <><div className="collection-bar"><div><h2>探索餐廳</h2><span><b>{visible.length}</b> / {data.restaurants.length} 間餐廳 <i/> <b>{mapped.length}</b> 間已定位 <i/> {visible.length-mapped.length} 間待定位</span></div><p>資料快照 · <time dateTime={data.fetchedAt}>{new Intl.DateTimeFormat('zh-HK',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Asia/Hong_Kong'}).format(new Date(data.fetchedAt))}</time></p></div><section className="filters" aria-label="搜尋與篩選"><label className="search-field">搜尋餐廳<input type="search" value={filters.query} onChange={e=>updateFilters({...filters,query:e.target.value})} placeholder="餐廳名稱或地址（中／英文）" /></label><label>地區<select value={filters.district} onChange={e=>updateFilters({...filters,district:e.target.value})}><option value="">所有地區</option>{districts.map(value=><option key={value}>{value}</option>)}</select></label><label>菜式<select value={filters.cuisine} onChange={e=>updateFilters({...filters,cuisine:e.target.value})}><option value="">所有菜式</option>{cuisines.map(value=><option key={value}>{value}</option>)}</select></label><button className="clear-filters" onClick={clearFilters} disabled={!filters.query&&!filters.district&&!filters.cuisine}>清除篩選</button><p className="result-summary" aria-live="polite" aria-atomic="true">找到 {visible.length} 間餐廳 · 地圖顯示 {mapped.length} 間{visible.length>mapped.length?' · 待定位餐廳可於名單查看詳情':''}</p></section><div className="mobile-tabs" aria-label="瀏覽方式"><button aria-pressed={view==='list'} onClick={()=>setView('list')}>餐廳名單</button><button aria-pressed={view==='map'} onClick={()=>setView('map')}>地圖探索</button></div><section className={`explorer show-${view}`} aria-label="餐廳地圖與名單"><aside className="list-panel" id="restaurant-list" tabIndex={-1}><div className="list-hint">你的下一站 <span>選擇餐廳以查看詳情 ↗</span></div><ul>{visible.map((r,i)=><li key={r.id} className={selected===r.id?'active':''}><button className="restaurant-button" aria-pressed={selected===r.id} onClick={()=>select(r,true)}><span className="restaurant-number">{String(i+1).padStart(2,'0')}</span><span className="restaurant-summary"><span className="restaurant-meta">{r.district || '香港'}{r.cuisine?` · ${r.cuisine}`:''}</span><span className="restaurant-name">{r.name}</span>{r.nameEn && r.nameEn!==r.name && <span className="restaurant-en">{r.nameEn}</span>}<span className="restaurant-address">{r.address}</span><span className="tags">{!hasCoordinates(r)?<span className="unmapped">待定位</span>:<span>已定位</span>}{r.isInHotel&&<span>酒店餐廳</span>}{r.isNew&&<span>新登場</span>}</span></span><span className="card-arrow" aria-hidden="true">↗</span></button>{selected===r.id && !hasCoordinates(r) && <Details r={r}/>}</li>)}</ul>{visible.length===0&&<div className="empty"><h3>找不到符合條件的餐廳</h3><p>試試其他名稱、地址，或放寬地區及菜式篩選。</p><button onClick={clearFilters}>顯示所有餐廳</button></div>}</aside><div className="map-panel" aria-label="香港餐廳地圖"><div className="map-actions"><span><span className="legend-dot"/> 餐飲禮遇餐廳</span><button onClick={()=>{setReset(n=>n+1);setSelected(null);}}>⌖ 重設視野</button></div>{tileError&&<div className="tile-notice" role="status">地圖底圖暫時無法載入；餐廳名單及詳情仍可使用。<button onClick={()=>{setTileError(false);setAttempt(n=>n+1);}}>重試</button></div>}<MapContainer center={[22.31,114.17]} zoom={11} scrollWheelZoom={false} className="map" aria-label="餐廳位置"><TileLayer key={attempt} attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" eventHandlers={{tileerror:()=>setTileError(true)}}/><ViewControl restaurants={visible} reset={reset}/><ClusteredPins restaurants={mapped} selected={selected} revision={revision} onSelect={select} details={r=><Details r={r}/>}/></MapContainer>{mapped.length===0&&<div className="map-empty">{visible.length===0?'沒有符合條件的地圖標記':'符合條件的餐廳尚待定位，請查看名單地址。'}</div>}<div className="map-caption">以一席美味，重新認識香港。<span>位置僅供參考 · 請以餐廳地址為準</span></div></div></section>{active && hasCoordinates(active) && <section className="accessible-details" aria-label="已選餐廳詳情" aria-live="polite"><Details r={active}/></section>}</>}
 <footer><div><strong>出發之前，先確認禮遇。</strong><p>本網站為非官方獨立指南，與 American Express／美國運通並無關聯。餐廳名單及禮遇或會更改；適用卡種、預訂、付款方式及其他條款，均以美國運通及餐廳最新公布為準。地圖座標僅供參考，並不保證入口位置。</p></div><External href={AMEX_URL}>查看官方條款</External></footer></main></>;
}
createRoot(document.getElementById('root')!).render(<StrictMode><App/></StrictMode>);
